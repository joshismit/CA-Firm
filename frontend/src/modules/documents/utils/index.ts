// documents-scoped pure helper functions.

/** Moved to the shared lib since the generic upload components (src/components/upload) need it too - re-exported here so existing call sites in this module are unaffected. */
export { formatFileSize } from '@/lib/utils'
